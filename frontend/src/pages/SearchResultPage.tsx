import { AlsoAvailable, SearchResult } from "../component";

const SearchResultPage = () => {
  return (
    <div>
      <SearchResult />
      <AlsoAvailable />
    </div>
  );
};

export default SearchResultPage;
